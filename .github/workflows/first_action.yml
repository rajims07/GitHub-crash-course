name: first_workflow

on:
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print greetings
        run: echo "Hello world"

      - name: Print good bye
        run: echo "Done - bye"
